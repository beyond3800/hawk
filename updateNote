package migrations

import "github.com/beyond3800/hawk/core/migration"

func init() {
	migration.Register(
		"create_users_table",
		func() error {
			return migration.DB.Exec(`
				CREATE TABLE users (
					id BIGINT AUTO_INCREMENT PRIMARY KEY,
					name VARCHAR(255),
					email VARCHAR(255)
				)
			`)
		},
		func() error {
			return migration.DB.Exec(
				"DROP TABLE users",
			)
		},
	)
}
package console

// This file is generated by Hawk. You can modify it, but be aware that it may be overwritten if you regenerate the console commands.
// The Seed function is registered as a console command and can be executed using the command line interface.
import (
	"fmt"
	"{{.Name}}/database/seeders"
)


func Seed() error {
	err := seeders.DatabaseSeeder{}.Run()
	if err == nil {
		fmt.Println("Database seeded successfully")
	}
	return err
}

func init() {
	Register("db:seed", Seed)
}